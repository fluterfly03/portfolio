import 'package:flutter/material.dart';

class AppColors {
  // Dark Backgrounds
  static const Color backgroundStart = Color(0xFF0D0B08);
  static const Color backgroundEnd = Color(0xFF050505);
  static const Color surfaceColor = Color(0xFF171412);

  // Neon accents (Orange & shades of orange)
  static const Color primary = Color(0xFFFF6B00);
  static const Color secondary = Color(0xFFFF9E40);
  static const Color accentGlow = Color(0xFFFFD180);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFBF7);
  static const Color textSecondary = Color(0xFFAFA090);

  // Utility colors
  static const Color glassBorder = Color(0x28FF6B00);
  static const Color glassBackground = Color(0x0CFFFFFF);

  // Dark/Light toggle
  static const Color cardBg = Color(0xFF1F1A15);
  static const Color activeGreen = Color(0xFF10B981);
}
